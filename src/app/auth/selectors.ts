import { RootState } from '../store';

export const auth = (state: RootState) => state.auth;
export const authMessage = (state: RootState) => state.auth;
export const authStatus = (state: RootState) => state.auth;
